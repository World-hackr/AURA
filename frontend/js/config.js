// AURA Configuration

const AURA = {

    // physical grid unit
    GRID_UNIT_MM: 0.635,

    // screen display size of one grid unit
    GRID_PIXEL_SIZE: 10,

    // texture scale (IMPORTANT TEST VALUE)
    TEXTURE_PIXELS_PER_GRID_UNIT: 40,

    // viewport size (window size, not workspace size)
    VIEWPORT_WIDTH_PX: 1200,
    VIEWPORT_HEIGHT_PX: 800,

    // texture file
    CURRENT_TEXTURE: "assets/textures/texture4.jpg",

    THEME: {
        BACKGROUND: "#202124",
        GRID_COLOR: "#ffffff22"
    }

};